version: '2'
services:
  go-feed:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=go.timos.design
      - LETSENCRYPT_HOST=go.timos.design
